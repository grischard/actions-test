name: Test Curl macOS
on:
  - push
jobs:
  test:
    name: Test Curl macOS
    runs-on: macos-latest
    steps:
    - name: Test curl macOS
      run: |
        curl -v https://join.osmfoundation.org/xmlrpc.php
        curl -v https://join.osmfoundation.org/xmlrpc.php

