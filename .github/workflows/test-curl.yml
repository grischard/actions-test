name: Test Curl
on:
  - push
jobs:
  test:
    name: Test Curl
    runs-on: ubuntu-latest
    steps:
    - name: Test curl
      run: |
        sudo ip addr
        sudo ifconfig
        sudo ip route
        sudo curl https://join.osmfoundation.org 
        sudo curl https://join.osmfoundation.org 

